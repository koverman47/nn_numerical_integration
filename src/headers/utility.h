#ifndef UTILITY_H
#define UTILITY_H

#include <cstdlib>
#include <vector>


int uniformInt(int lower, int upper = nullptr);
double UnifromDouble(double lower, double upper = nullptr);
double maximize(vector<double> vec);


#endif
